export const meta = {
  // DRAFT copy. Describe-only: client assets are likely not showable. State the
  // specific contribution precisely; show nothing internal.
  slug: "horton-group",
  index: 4,
  title: "Horton Group\nPlatform",
  year: "2017",
  kind: "Platform",
  summary:
    "A reusable client platform built to be re-skinned, plus a remediation that pulled a bloated site back to maintainable. Architecting for reuse and rescue.",
  role: ["Architecture", "Full-stack", "Remediation"],
  stack: ["Angular", "KeystoneJS", "Node.js"],
  status: "Delivered",
  media: [],
  context: ["With Horton Group's senior developer", "Agency engagement"],
  outcomes: [
    "Templated app, re-skinnable per client",
    "Reuse designed in from the start",
    "WordPress remediation to maintainable",
  ],
};

## The brief

Two jobs, one theme. The first was a templated application built with Horton's senior
developer on Angular and KeystoneJS, designed so the team could re-skin it for future
clients instead of rebuilding from scratch each time. The second was a remediation:
taking a bloated WordPress site (Sisters on the Fly) back to a state someone could
actually maintain.

## The work

On the platform, the interesting decisions were about the seams: what to make
configurable, what to keep fixed, and how to keep a single codebase serving many
client skins without forking. On the remediation, it was triage and discipline,
cutting accumulated weight without breaking what the site needed to keep doing.

## Why it belongs here

Designing for reuse and rescuing a system that has drifted are both senior signals.
Most of the value was in the architecture, not the surface, which is why this is
described rather than shown.
